import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'web.app.rodrigosancho8e447',
  appName: 'Portfolio',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
